  on:
    push:
      branches: [main, prod]
    pull_request:
      branches: [main, prod]
    workflow_dispatch:

  jobs:
    basic-check:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - run: echo "CI is running"
